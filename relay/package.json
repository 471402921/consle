{
  "name": "@cute/relay",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.cjs",
    "build": "tsup src/server.ts --format cjs --target node20 --external ws --clean --no-dts",
    "typecheck": "tsc --noEmit",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@cute/shared": "*",
    "@types/node": "^22.9.0",
    "@types/ws": "^8.5.13",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
